
  <% categoria.productos.each do |producto| %>
    <li class='imageThumb'>
      <a href = "<%= producto.imagen.url(:medium) %>" rel='prettyPhoto[gallery]' title="<%= producto.descripcion %>"><%= image_tag producto.imagen.url(:thumb) %></a>
    </li>
  <% end %>


<script type ="text/javascript">
	jQuery(document).ready(function(){
	  jQuery("area[rel^='prettyPhoto']").prettyPhoto();
	  jQuery(".gallery:first a[rel^='prettyPhoto']").prettyPhoto({theme:'light_square'});
	  jQuery(".gallery:gt(0) a[rel^='prettyPhoto']").prettyPhoto({animation_speed:'fast',slideshow:10000, hideflash: true});

	  jQuery(".gallery a[rel^='prettyPhoto']:first").prettyPhoto({
	    custom_markup: '<div id="map_canvas" style="width:500px;"></div>',
	    changepicturecallback: function(){ initialize(); }
	  });
        });
</script>
